general:
  artifacts:
    - demo
test:
  override:
    - echo "No Tests"
deployment:
  demo:
    branch: /.*/
    commands:
      - npm run build
      - echo "Submitting PR Comment..."
      - ./demo/bot.js